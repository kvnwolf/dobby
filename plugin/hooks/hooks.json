{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "sh -c '[ -f dobby.config.json ] && [ -x node_modules/.bin/dobby ] && exec node_modules/.bin/dobby check --hook || exit 0'"
          }
        ]
      }
    ]
  }
}

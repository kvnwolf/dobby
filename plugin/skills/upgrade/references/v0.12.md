# v0.12.0 — fixed native Workflow worker recipe

## Action: inspect the effective recipe

Run:

```bash
bunx dobby env --json
```

Read the complete `workflowRecipe` object. Its id is `baseline-v1`; `/dobby:execute` passes that object unchanged into the existing native Claude Workflow and starts no second Claude process. There is one recipe for every goal and ad-hoc task during the seven-day experiment:

| Role | Model | Reasoning |
| --- | --- | --- |
| researcher | `claude-sonnet-5` | `medium` |
| test-author | `claude-opus-5` | `high` |
| implementor | `claude-sonnet-5` | `high` |
| reviewer | `claude-opus-5` | `high` |
| verifier | `claude-sonnet-5` | `medium` |

The build limits are `maxOuter: 2` and `maxConcurrency: 2`; verification is mechanical-first. Normal execute is optional test-author → implementor → verifier and has no `maxReview`. A first verification failure may dispatch one fix because another verifier attempt remains; the final failure stops `needs-human` without a terminal fix. The reviewer stays available for explicit dispatch and missing-work-log safety review. Holistic static review moves to the external PR boundary and must cover the current HEAD. Telemetry records the recipe id, attempts, retries, first-attempt success, exhausted caps, outcomes, and verification source; runtime usage fields Claude does not expose remain `unknown`.

Do not add recipe fields to `dobby.config.json`, edit role files to tune one run, export model/budget environment variables, or pass model/profile flags. `cli/src/workflow-recipe.ts` is the canonical worker model/effort/limit map, and agent frontmatter is a mechanically checked direct-call mirror. The main-thread Architect keeps the interactive session's manually selected model/effort and is not a recipe role.

## Action: keep the seven-section state contract

New sessions use the original seven canonical sections:

```text
Goal → Source → Exploration → Findings (interview) → Research → Spec → Work log
```

Do not create `## Execution profile` and do not prompt for Economical, Standard, or Critical. If an older active `STATE.md` already contains `## Execution profile`, leave it alone: the section engine preserves and tolerates it as unknown legacy data, while `baseline-v1` ignores it.

## Experiment checkpoint

Use `baseline-v1` unchanged for seven days of real work, then inspect first-attempt success, retries, cap exhaustion, outcomes, and mechanical-versus-model verification. Change the single canonical recipe only from that evidence; profiles and automatic task-level intelligence selection remain explicitly deferred.
